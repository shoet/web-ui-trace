import type { Meta, StoryObj } from "@storybook/react";
import { RenameAccount } from ".";

export default {
  title: "settings/RenameAccount",
  component: RenameAccount,
} as Meta<typeof RenameAccount>;

type Story = StoryObj<typeof RenameAccount>;

export const Default: Story = {};
