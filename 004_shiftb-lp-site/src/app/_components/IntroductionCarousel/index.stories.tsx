import type { Meta, StoryObj } from "@storybook/react";
import { IntroductionCarousel } from ".";

export default {
  title: "IntroductionCarousel",
  component: IntroductionCarousel,
} as Meta<typeof IntroductionCarousel>;

type Story = StoryObj<typeof IntroductionCarousel>;

export const Default: Story = {};
